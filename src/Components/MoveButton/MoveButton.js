import React from 'react';
import styles from './MoveButton.module.scss';

const MoveButton = () => {
    return (
        <div className={styles.MoveButton}>
            {/* TODO: to implement move button (Find bast way to pass an image or src) */}
            {/* TODO: Move image on src */}
            {/* "I create a folder called images in my source folder and put all my images there. And I can import them with say import imageName from '../images/imageName.png'" */}
            {/* https://stackoverflow.com/questions/53190989/where-should-i-put-images-files-in-react-project */}
        </div>
    );
}

export default MoveButton;
